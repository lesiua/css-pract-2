{{#with data}}
  <footer class='footer'>
    <div class='footer_links'>
      {{#each pages}}
        <a href='{{path}}' class='nav_link'>{{name}}</a>
      {{/each}}
    </div>
    <div>
      <span class='footer_copyright'>{{copyright}}</span>
    </div>
  </footer>
{{/with}}